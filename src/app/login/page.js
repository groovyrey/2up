import React from 'react';
import LoginPage from '../../components/LoginPage';

export const metadata = {
  title: "Login - 2up",
  description: "Login to your 2up account.",
};

export default function Login() {
  return <LoginPage />;
}
